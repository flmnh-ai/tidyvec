% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{calculate_map}
\alias{calculate_map}
\title{Calculate Mean Average Precision for a tidyvec collection}
\usage{
calculate_map(
  x,
  variable,
  k = 25,
  id_column = "id",
  similarity_column = "similarity",
  exclude_self = TRUE,
  progress = TRUE
)
}
\arguments{
\item{x}{A tidyvec collection}

\item{variable}{Column name to evaluate (e.g., "category", "label")}

\item{k}{Number of results to consider (default: 25)}

\item{id_column}{Column name for unique identifiers (default: "id")}

\item{similarity_column}{Column name for similarity scores (default: "similarity")}

\item{exclude_self}{Whether to exclude the query item from results (default: TRUE)}

\item{progress}{Whether to show progress bar (default: TRUE)}
}
\value{
Numeric value between 0 and 1 representing Mean Average Precision
}
\description{
Evaluates retrieval performance by using each item as a query and measuring
how well the model retrieves other items with the same attribute value.
}
\details{
For each item in the collection:
\enumerate{
\item Uses its embedding as a query
\item Retrieves k nearest neighbors
\item Calculates AP@k for items with matching variable value
\item Returns the mean of all AP scores
}

Items with missing or "null" values in the specified variable are excluded.
}
\examples{
\dontrun{
library(tidyvec)
library(dplyr)

# Create a collection with categories
data <- tibble(
  id = 1:100,
  category = sample(c("A", "B", "C"), 100, replace = TRUE),
  text = paste("Document", 1:100)
)

collection <- data \%>\%
  vec(embedding_fn = embedder_tfidf(data$text)) \%>\%
  embed(content_column = "text")

# Evaluate how well the model retrieves items by category
map_score <- calculate_map(collection, "category", k = 10)
print(paste("MAP:", round(map_score, 3)))
}

}
